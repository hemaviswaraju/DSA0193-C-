#include<iostream>
using namespace std;
int main() {
    int n = 153,sum=0,rem;
    int temp=n;
    while(n!=0){
        rem=n%10;
        sum+=rem*rem*rem;
        n=n/10;
    }
    cout<<(temp==sum?"Armstrong number":"Not an armstrong number");
    return 0;
}
